# README screenshots

Save the following PNGs in this directory so the images in the top-level
[README.md](../../README.md) render on GitHub. Filenames must match exactly.

| Filename | What it shows |
| --- | --- |
| `ci-pipeline-run.png` | A `github-ci.yml` run: init → validate/trivy → build → deploy |
| `trivy-finding.png` | The Trivy job reporting a HIGH/CRITICAL IaC finding |
| `terraform-destroy-run.png` | A successful `Terraform Destroy` workflow run |
| `s3-state-bucket.png` | The `infra-s3-bucket-89` state bucket in the S3 console |
